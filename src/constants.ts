// Kept separate from FastTextColorSettings so that the color layer does not
// have to import the settings module, which would form an import cycle.

export const CSS_COLOR_PREFIX = "ftc-color-"
export const VAR_COLOR_PREFIX = "--ftc-color-"
